package coding;

public class three {
	public static void main(String[] args) {
		int a = 1;
		int b = 2;
		int res;
		
		if(a > b) {
			res = 11;
		}else {
			res = 22;
		}
		
		res = a > b ? 11 : 22;
		System.out.println(res);
		
		//두숫자 a,b 와 하나의 문자 c 를 입력받는 코드
		// +를 입력받으면 두수의 합을 출력하고
		// /를 입력받으면 두수의 나눈값을 출력하고
		// 그 외의 값을 입력 받으면 wrong 이라는 문자열을 출력하는 코드
		char c;
		int result;
		//result = (c=='+') ? a+b : (c=='/') ? a/b : 1;
		
		
	}
}
